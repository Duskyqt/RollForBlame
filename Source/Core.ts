// Content //

C_Timer.After(2, () => {
    // Wow common example
    print("Hello", UnitName("Player"), "thank you for using", AddonName, "!");

    // Just an example of tstl method
    const x = Object.keys({});

    // Just an example luabox example
    RunMacroText("/jump");
});
